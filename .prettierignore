*.lock
bun.lock
pnpm-lock.yaml
build
coverage
dist
node_modules
src-tauri/binaries
src-tauri/gen
src-tauri/target

# The round-trip corpus. These files are deliberately malformed, unusually
# indented and CRLF-terminated; formatting them would erase exactly what they
# test. See src/markdown/roundtrip.test.ts.
src/markdown/fixtures

# Generated by release-please, which writes `*` bullets and a blank line after
# each version heading. Prettier rewrites both, so formatting this file produces
# a diff that the next release reverses — every release, forever.
CHANGELOG.md
