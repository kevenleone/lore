/**
 * View → Search, as a DOM event. The search box lives in `TitleBar` and owns the
 * input it has to focus, so the menu command is announced rather than routed.
 */
export const SEARCH_COMMAND = 'lore:search';
